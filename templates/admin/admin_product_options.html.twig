{% extends 'admin/admin_base.html.twig' %}

{% set can_edit = is_granted('product_option_edit') %}
{% set can_delete = is_granted('product_option_delete') %}

{% block admin_subheading_extra %}

    {% if can_edit %}
        <a href=" {{ path('admin_product_option_edit') }} " class="btn-floating btn-medium waves-effect waves-light light-blue"><i class="material-icons">add</i></a>
    {% endif %}

{% endblock %}

{% block admin_body %}

    <div class="row">
        <h4>Hledat</h4>

        {# Formulář - vyhledávací pole, řazení #}
        {{ include('fragments/forms_unique/_form_search_order.html.twig', {form: searchForm}) }}
    </div>

    <div class="row">
        <h4>Výsledky ({{ pagination.getTotalItems() }})</h4>

        {% if options is empty %}
            Hledání neodpovídají žádné produktové volby.
        {% else %}
            <div class="collection">
                {% for option in options %}
                    <div class="collection-item">
                        <strong>
                            {{ option.getName() }}
                        </strong>
                        - {{ option.getType() }}

                        {% if can_delete %}
                            <a href=" {{ path('admin_product_option_delete', {id: option.getId()}) }} " class="secondary-content">
                                <i class="material-icons">delete</i>
                            </a>
                        {% endif %}

                        {% if can_edit %}
                            <a href=" {{ path('admin_product_option_edit', {id: option.getId()}) }} " class="secondary-content">
                                <i class="material-icons">create</i>
                            </a>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    {# Pagination #}
    {{ include('fragments/_pagination.html.twig') }}

{% endblock %}