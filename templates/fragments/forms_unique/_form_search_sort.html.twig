{{ form_start(form) }}
    <div class="row">
        <div class="input-field col s12 l6">
            {{ form_widget(form.phrase.text) }}
            {{ form_label(form.phrase.text) }}
            {{ form_help(form.phrase.text) }}
            {{ form_errors(form.phrase.text) }}
        </div>

        <div class="input-field col s12 l4">
            {{ form_widget(form.sort.sortBy) }}
            {{ form_label(form.sort.sortBy) }}
            {{ form_help(form.sort.sortBy) }}
            {{ form_errors(form.sort.sortBy) }}
        </div>

        <div class="input-field col s12 l2">
            <div class="col s12">
                <div class="row">
                    <button class="btn waves-effect waves-light blue width-max no-padding center-align" type="submit">
                        Hledat
                    </button>
                </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}