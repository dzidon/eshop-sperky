{% extends 'base.html.twig' %}

{% block body %}

    {# Breadcrumbs #}
    {{ include('fragments/_breadcrumbs.html.twig') }}

    <div class="container">

        {# h1 #}
        {{ include('fragments/_h1.html.twig', {route: app.request.get('_route')}) }}

        {# Flash zprávy #}
        {{ include('fragments/_flash_messages.html.twig') }}

        <div class="section">
            <div class="row">
                <div class="row center-align">Opravdu si přejete smazat svou recenzi? Tato akce je nevratná.</div>
                {{ form(reviewDeleteForm) }}
            </div>
        </div>
    </div>

{% endblock %}