{% extends 'base.html.twig' %}

{% block body %}

    {# Breadcrumbs #}
    {{ include('fragments/_breadcrumbs.html.twig') }}

    <div class="container">
        <div class="container">
            <h1>{{ breadcrumbs.getPageTitle() }}</h1>

            {# Flash zprávy #}
            {{ include('fragments/_flash_messages.html.twig') }}

            <div class="section">
                {{ form(reviewForm) }}

                {% if review.getId() is not null and is_granted('review_delete', review) %}
                    <div class="row center-align">
                        <a href="{{ path('review_delete', {id: review.getId()}) }}">Smazat</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

{% endblock %}
